name: Sign macOS Artifacts
permissions:
  contents: read

on:
  workflow_call:
    inputs:
      version:
        description: 'Version to sign'
        required: true
        type: string
    secrets:
      MACOS_CERT_P12_BASE64:
        description: 'Base64 encoded p12 certificate'
        required: false
      MACOS_CERT_P12_PASSWORD:
        description: 'Password for p12 certificate'
        required: false
      MACOS_SIGNING_IDENTITY:
        description: 'Code signing identity'
        required: false
      MACOS_NOTARY_APPLE_ID:
        description: 'Apple ID for notarization'
        required: false
      MACOS_NOTARY_PASSWORD:
        description: 'App-specific password for notarization'
        required: false
      MACOS_NOTARY_TEAM_ID:
        description: 'Team ID for notarization'
        required: false
    outputs:
      signed:
        description: 'Whether macOS artifacts were code-signed and notarized'
        value: ${{ jobs.sign-macos.outputs.signed }}

jobs:
  sign-macos:
    runs-on: macos-latest
    outputs:
      signed: ${{ steps.validate.outputs.should_sign }}
    strategy:
      matrix:
        arch: [aarch64, x86_64]
    
    steps:
      - name: Validate signing secrets
        id: validate
        run: |
          MISSING_SECRETS=()
          
          if [ -z "${{ secrets.MACOS_CERT_P12_BASE64 }}" ]; then
            MISSING_SECRETS+=("MACOS_CERT_P12_BASE64")
          fi
          if [ -z "${{ secrets.MACOS_CERT_P12_PASSWORD }}" ]; then
            MISSING_SECRETS+=("MACOS_CERT_P12_PASSWORD")
          fi
          if [ -z "${{ secrets.MACOS_SIGNING_IDENTITY }}" ]; then
            MISSING_SECRETS+=("MACOS_SIGNING_IDENTITY")
          fi
          if [ -z "${{ secrets.MACOS_NOTARY_APPLE_ID }}" ]; then
            MISSING_SECRETS+=("MACOS_NOTARY_APPLE_ID")
          fi
          if [ -z "${{ secrets.MACOS_NOTARY_PASSWORD }}" ]; then
            MISSING_SECRETS+=("MACOS_NOTARY_PASSWORD")
          fi
          if [ -z "${{ secrets.MACOS_NOTARY_TEAM_ID }}" ]; then
            MISSING_SECRETS+=("MACOS_NOTARY_TEAM_ID")
          fi
          
          if [ ${#MISSING_SECRETS[@]} -gt 0 ]; then
            echo "::warning::⚠️ macOS code signing will be skipped - missing secrets: ${MISSING_SECRETS[*]}"
            echo "The release will continue with unsigned macOS artifacts."
            echo "should_sign=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          
          echo "should_sign=true" >> "$GITHUB_OUTPUT"
          echo "✅ All required secrets are present"

      - uses: actions/checkout@v4
        if: steps.validate.outputs.should_sign == 'true'

      - name: Download macOS artifacts
        if: steps.validate.outputs.should_sign == 'true'
        uses: actions/download-artifact@v4
        with:
          name: ANUMCL_${{ inputs.version }}_macos_${{ matrix.arch }}
          path: artifacts

      - name: Setup signing environment
        if: steps.validate.outputs.should_sign == 'true'
        env:
          CERT_P12_BASE64: ${{ secrets.MACOS_CERT_P12_BASE64 }}
          CERT_P12_PASSWORD: ${{ secrets.MACOS_CERT_P12_PASSWORD }}
        run: |
          # Decode p12 file
          echo "$CERT_P12_BASE64" | base64 -d > /tmp/cert.p12
          
          # Create temporary keychain
          KEYCHAIN_PATH=$HOME/temp-signing.keychain-db
          security create-keychain -p "password" "$KEYCHAIN_PATH"
          security unlock-keychain -p "password" "$KEYCHAIN_PATH"
          security set-keychain-settings "$KEYCHAIN_PATH"
          
          # Import certificate
          security import /tmp/cert.p12 -k "$KEYCHAIN_PATH" -P "$CERT_P12_PASSWORD" -T /usr/bin/codesign
          
          # Set default keychain
          security list-keychains -s "$KEYCHAIN_PATH"
          
          # Clean up certificate file
          rm /tmp/cert.p12

      - name: Install appdmg
        if: steps.validate.outputs.should_sign == 'true'
        run: |
          npm install -g appdmg

      - name: Extract and sign .app
        if: steps.validate.outputs.should_sign == 'true'
        env:
          SIGNING_IDENTITY: ${{ secrets.MACOS_SIGNING_IDENTITY }}
        working-directory: artifacts
        run: |
          ENTITLEMENTS_PATH="${GITHUB_WORKSPACE}/src-tauri/Entitlements.plist"
          [ -f "$ENTITLEMENTS_PATH" ]

          # Extract the .app from tar.gz
          tar -xzf ANUMCL_${{ inputs.version }}_macos_${{ matrix.arch }}.app.tar.gz
          
          # Sign the .app bundle
          codesign --deep --force --options runtime \
            --entitlements "$ENTITLEMENTS_PATH" \
            --sign "$SIGNING_IDENTITY" \
            "ANUMCL.app"
          
          # Verify signing
          codesign --verify --deep --strict --verbose=2 ANUMCL.app
          
          # Re-compress the signed .app
          rm ANUMCL_${{ inputs.version }}_macos_${{ matrix.arch }}.app.tar.gz
          tar -czf ANUMCL_${{ inputs.version }}_macos_${{ matrix.arch }}.app.tar.gz ANUMCL.app

      - name: Create DMG configuration
        if: steps.validate.outputs.should_sign == 'true'
        working-directory: artifacts
        run: |
          # Create appdmg configuration file
          cat > dmg-config.json << EOF
          {
            "title": "ANUMCL Installer",
            "icon": "../src-tauri/assets/icons/dmg/volume.icns",
            "background": "../src-tauri/assets/misc/dmg_bg.png",
            "format": "UDZO",
            "icon-size": 120,
            "window": {
              "size": {
                "width": 640,
                "height": 418
              }
            },
            "contents": [
              {
                "x": 180,
                "y": 170,
                "type": "file",
                "path": "ANUMCL.app"
              },
              {
                "x": 480,
                "y": 170,
                "type": "link",
                "path": "/Applications"
              }
            ]
          }
          EOF

      - name: Create and sign DMG
        if: steps.validate.outputs.should_sign == 'true'
        env:
          SIGNING_IDENTITY: ${{ secrets.MACOS_SIGNING_IDENTITY }}
        working-directory: artifacts
        run: |
          # Remove the original DMG if it exists (build workflow no longer creates DMG)
          rm -f ANUMCL_${{ inputs.version }}_macos_${{ matrix.arch }}.dmg
          
          # Create a new DMG with the signed app using appdmg
          appdmg dmg-config.json ANUMCL_${{ inputs.version }}_macos_${{ matrix.arch }}.dmg
          
          # Sign the new DMG
          codesign --force --sign "$SIGNING_IDENTITY" \
            ANUMCL_${{ inputs.version }}_macos_${{ matrix.arch }}.dmg
          
          # Verify DMG signing
          codesign --verify --deep --strict --verbose=2 ANUMCL_${{ inputs.version }}_macos_${{ matrix.arch }}.dmg
          
          # Clean up extracted .app and config file
          rm -rf ANUMCL.app dmg-config.json

      - name: Notarize .app
        if: steps.validate.outputs.should_sign == 'true'
        env:
          NOTARY_APPLE_ID: ${{ secrets.MACOS_NOTARY_APPLE_ID }}
          NOTARY_PASSWORD: ${{ secrets.MACOS_NOTARY_PASSWORD }}
          NOTARY_TEAM_ID: ${{ secrets.MACOS_NOTARY_TEAM_ID }}
        working-directory: artifacts
        run: |
          # Extract .app for notarization
          tar -xzf ANUMCL_${{ inputs.version }}_macos_${{ matrix.arch }}.app.tar.gz
          
          # Create zip for notarization
          ditto -c -k --keepParent ANUMCL.app ANUMCL.zip
          
          # Submit for notarization
          xcrun notarytool submit ANUMCL.zip \
            --apple-id "$NOTARY_APPLE_ID" \
            --password "$NOTARY_PASSWORD" \
            --team-id "$NOTARY_TEAM_ID" \
            --wait
          
          # Staple the notarization
          xcrun stapler staple ANUMCL.app
          
          # Re-compress the notarized .app
          rm ANUMCL_${{ inputs.version }}_macos_${{ matrix.arch }}.app.tar.gz ANUMCL.zip
          tar -czf ANUMCL_${{ inputs.version }}_macos_${{ matrix.arch }}.app.tar.gz ANUMCL.app
          rm -rf ANUMCL.app

      - name: Notarize DMG
        if: steps.validate.outputs.should_sign == 'true'
        env:
          NOTARY_APPLE_ID: ${{ secrets.MACOS_NOTARY_APPLE_ID }}
          NOTARY_PASSWORD: ${{ secrets.MACOS_NOTARY_PASSWORD }}
          NOTARY_TEAM_ID: ${{ secrets.MACOS_NOTARY_TEAM_ID }}
        working-directory: artifacts
        run: |
          # Submit DMG for notarization
          xcrun notarytool submit ANUMCL_${{ inputs.version }}_macos_${{ matrix.arch }}.dmg \
            --apple-id "$NOTARY_APPLE_ID" \
            --password "$NOTARY_PASSWORD" \
            --team-id "$NOTARY_TEAM_ID" \
            --wait
          
          # Staple the notarization to DMG
          xcrun stapler staple ANUMCL_${{ inputs.version }}_macos_${{ matrix.arch }}.dmg

      - name: Verify notarization
        if: steps.validate.outputs.should_sign == 'true'
        working-directory: artifacts
        run: |
          # Extract the notarized .app for verification
          tar -xzf ANUMCL_${{ inputs.version }}_macos_${{ matrix.arch }}.app.tar.gz
          
          echo "=== Testing Gatekeeper assessment on notarized app ==="
          spctl --verbose=4 --assess --type execute ANUMCL.app
          
          echo "=== Final signature verification ==="
          codesign --verify --deep --strict --verbose=2 ANUMCL.app

      - name: Calculate SHA256
        if: steps.validate.outputs.should_sign == 'true'
        working-directory: artifacts
        run: |
          CORE_EXECUTABLE="ANUMCL.app/Contents/MacOS/ANUMCL"
          if [ -f "$CORE_EXECUTABLE" ]; then
            EXECUTABLE_SHA256=$(shasum -a 256 "$CORE_EXECUTABLE" | awk '{print $1}')
            echo "Signed core executable SHA256: $EXECUTABLE_SHA256"
            ARTIFACT_NAME="ANUMCL_${{ inputs.version }}_macos_${{ matrix.arch }}_signed"
            echo "$EXECUTABLE_SHA256  $ARTIFACT_NAME" > "$ARTIFACT_NAME.sha256"
          fi

      - name: Cleanup artifacts
        if: steps.validate.outputs.should_sign == 'true'
        working-directory: artifacts
        run: |
          rm -rf ANUMCL.app
          ls -la .

      - name: Cleanup keychain
        if: always() && steps.validate.outputs.should_sign == 'true'
        run: |
          KEYCHAIN_PATH=$HOME/temp-signing.keychain-db
          if [ -f "$KEYCHAIN_PATH" ]; then
            security delete-keychain "$KEYCHAIN_PATH"
          fi

      - name: Upload signed artifacts
        if: steps.validate.outputs.should_sign == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: ANUMCL_${{ inputs.version }}_macos_${{ matrix.arch }}_signed
          path: artifacts/*
          if-no-files-found: error
